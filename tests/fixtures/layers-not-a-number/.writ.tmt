@kind(writ)

#[ Writ: layers-not-a-number fixture ]

##[ crate-layering ]

@rule(crate-layering){
  guard: { twrit: layers }

  layers: {
    low: [ "thing" ]
  }
}

A layer key has to be a number, because the whole rule is an ordering.
