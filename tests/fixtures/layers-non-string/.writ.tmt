@kind(writ)

#[ Writ: layers-non-string fixture ]

##[ crate-layering ]

@rule(crate-layering){
  guard: { twrit: layers }

  layers: {
    1: [ 42 ]
  }
}

Patterns are directory paths, so every entry has to be a string.
