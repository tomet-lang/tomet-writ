@kind(writ)

#[ Writ: layers-twice fixture, nested ]

##[ crate-layering ]

A second declaration, in a writ further down. A layer order is a
property of the workspace, so there can only be one of it.

@rule(crate-layering){
  guard: { twrit: layers }

  layers: {
    0: [ "other" ]
  }
}
