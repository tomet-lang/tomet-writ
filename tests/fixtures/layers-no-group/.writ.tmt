@kind(writ)

#[ Writ: layers-no-group fixture ]

##[ crate-layering ]

@rule(crate-layering){ guard: { twrit: layers } }

The declaration carries no `{...}`, so there is nothing to read. Written
with empty `(...)` because a bare `@layers` is not seen as an element at
all -- see `layers-bare`.
